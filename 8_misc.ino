/*
  Miscellaneous commands
*/
int numLoops = 0, looptimer = 0;



void loopscount() {
  if (millis() - looptimer > 1000) {
    Serial.println(numLoops);
    looptimer = millis();
    numLoops = 0;
  }
  else numLoops++;
}



void delayfunc(String func, int del) {      //delays with countdown in serial monitor, was used to check current consumption
  Serial.print(func);
  Serial.print(" beginning in ");
  for (int i = del ; i > 0; i--) {
    Serial.print(i);
    delay(300);
    Serial.print(".");
    delay(300);
    Serial.print(".");
    delay(400);
  }
  Serial.println("");
}



//void TFTPrint3() {
//  tft.setTextPadding(20);
//  tft.setTextDatum(MC_DATUM);
//  tft.drawString("Screen 3", 120, 60);
//  tftupdate = 0;
//  Serial.println("TEST");
//}



void TFTPrintError() {
  tft.setTextDatum(TL_DATUM);
  tft.setTextPadding(20);
  String errormsg = "Screen ";
  errormsg += String(screen);
  errormsg += " not defined";
  tft.drawString("Screen not defined", 0, 10, 4);
}


/*

  int serialPrintLocalTime() {          //serial print current time, returns 1 if error
  if (!getLocalTime(&timeinfo)) {
    Serial.println("Failed to obtain time");
    TFTPrintTime();
    syncWiFi();
    return 1;
  }
  Serial.println(&timeinfo, "%A, %B %d %Y %H:%M:%S");
  return 0;
  }

*/
